// Дано два числа, выполнить операции умножения, деления и сложения (Mul, Div, Sum)

const multiply = (x, y) => x * y;

const divide = (x, y) => x / y;

const sum = (x, y) => x + y;

module.exports = {
  multiply,
  divide,
  sum,
};
