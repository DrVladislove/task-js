const operations = require("./operations-loops-2.js");
const assert = require("assert");
it("Таблица умножения выполнена правильно loops-task-2", () => {
  assert.equal(operations.multiplicationTable(), "1*1=1");
});
